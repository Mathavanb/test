<p style="color: green"><%= notice %></p>
<!DOCTYPE html>
<html>
  <head>
    <title>Test1</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>

  </head>

  <body>
    <%= yield %>
  <% if user_signed_in? %>
    <div style="position:absolute;top:40px;right:30px"><strong><%= current_user.email %></strong></div>
    <div style="position:absolute;top:70px;right:40px"><%= button_to 'Log Out', destroy_user_session_path, method: :delete %></div>
    <div style="position:absolute;top:70px;right:120px"><%= link_to 'Edit profile', edit_user_registration_path %></div>
    <% end %>
  </body>
</html>
