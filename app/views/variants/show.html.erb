<p id="notice"><%= notice %></p>

<p>
  <strong>Colour:</strong>
  <%= @variant.colour %>
</p>

<p>
  <strong>Price:</strong>
  <%= @variant.price %>
</p>

<p>
  <strong>Product:</strong>
  <%= @variant.product_id %>
</p>

<%= link_to 'Edit', edit_product_variant_path(@product,@variant) %> |
<%= link_to 'Back', product_variants_path(@product) %>
